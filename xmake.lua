set_languages("cxx17")
add_requires("catch2")
target("lib")
    set_kind("headeronly")
    add_headerfiles("lib/include/logger.h")
target("examples")
    set_kind("binary")
    add_files("examples/*.cpp")
    add_includedirs("lib/include")
    add_ldflags("-pthread")
    add_deps("lib")
